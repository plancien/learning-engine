define(['event_bus', 'connector', 'modules/frames', 'modules/key_listener', 'modules/events_list'], function (eventBus, socket) {
    
    var game = {};
    
    game.init = function () {
    };
    
    return game; 
    
});